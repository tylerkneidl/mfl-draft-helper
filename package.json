{
  "name": "mfl-copilot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "inspect": "node --env-file-if-exists=.env src/inspect.mjs",
    "start": "node --env-file-if-exists=.env src/server.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "fastify": "^5.8.5"
  }
}
